# 参与贡献

欢迎改进这个使用虚构设备模型的冷站遗传算法示例。

- Issue 请提供 Python 版本、复现命令、错误信息和可公开的最小配置/负荷。
- 不提交现场数据、实测拟合参数、连接凭据、私有设备名称或运行输出。示例必须使用虚构数据。
- 修改模型时说明单位、适用假设和物理约束；新增配置同步更新 README。
- 修改优化逻辑时，保留硬约束复核和无可行解的明确错误，不用降低负荷或放宽边界掩盖问题。
- PR 附上 `python -m unittest discover -s tests -v` 的验证结果，以及与改动相关的最小复现。

项目仅依赖 Python 标准库。增加依赖前，请说明实际需要。贡献代码以项目 MIT 许可证提供。
